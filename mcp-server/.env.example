# Base URL of the DB Chat MCP API (note the /api/mcp/v1 suffix).
DBCHAT_API_URL=https://crm.srv879786.hstgr.cloud/api/mcp/v1

# A bearer key minted in DB Chat → Admin Settings → MCP Tools (shown once).
DBCHAT_API_KEY=fck_live_PASTE_YOUR_KEY
